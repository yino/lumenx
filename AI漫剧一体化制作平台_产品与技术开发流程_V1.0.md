# AI 漫剧一体化制作平台：产品与技术开发流程

> 文档版本：V1.0（登录态竞品复核与技术路线修订稿）  
> 编写日期：2026-08-08  
> 本次修订：完成 Tago Movie 登录后工作台、创作入口、参数、资产中心与订阅能力的只读复核；根据项目维护者掌握 Java、PHP、Go 的实际情况，将后端主路线由 NestJS 调整为 Go，并补充 FFmpeg 使用边界和 Java/PHP 备选方案。  
> 开发基线：用户确认本稿后，按本文的模块边界、数据模型、接口抽象和里程碑开始开发  
> 参考项目：《一卒定山河》第 2—10 集实际制作、重制、QA 与流程优化资料  
> 参考产品：[Tago Movie](https://tagomovie.bitaihub.com/)  
> 竞品复核边界：已使用登录态检查 `/app`、`/projects`、`/assets` 和订阅弹窗；为避免创建外部数据或消耗积分，本次没有创建测试项目，因此项目内部的画布、生成器和剪辑器细节仍只记录为“待实测”，不根据套餐文案推断具体交互。

---

## 1. 文档结论

本项目不是三个彼此孤立的工具页，而应做成一个围绕“项目—剧集—分段—镜头—素材—生成版本—时间线—成片”的一体化生产系统。

核心设计原则如下：

1. **剧情数据结构化**：剧本、证据链、人物状态、镜头、台词、提示词和字幕不是散落文本，而是可版本化、可校验、可回溯的数据对象。
2. **人物与世界观资产化**：人物脸、服装、伤势、道具、场景、光线、时间等形成“项目圣经”和连续状态表，后续每集继承，不重复手工说明。
3. **模型接入适配器化**：Seedance 2.0、未来可用的 Seedance 2.5、Grok 以及其他模型均通过统一 Provider Adapter 接入，前端和业务层不依赖某一家接口字段。
4. **生成任务异步化**：图片、视频、转码、抽帧、字幕、代理文件和最终导出全部进入可观测任务队列，支持重试、取消、超时、费用统计和失败段重做。
5. **剪辑非破坏化**：时间线只保存引用、入出点、转场和效果参数，不覆盖原素材；任何生成版本均可切换、对比和回滚。
6. **QA 前置且自动化**：在提交昂贵的视频生成前先做剧本、连续性、提示词、素材尺寸、时码和预算预检；生成后再做技术、视觉与剧情三层验收。
7. **优先打通闭环，再扩展高级功能**：第一版先实现一集从剧情到可导出 MP4 的完整闭环，不先做多人实时协作、复杂特效和移动端专业剪辑。

建议技术形态：**React + TypeScript Web 前端 + Go API + Go 异步 Worker + PostgreSQL + Redis/Asynq + S3 兼容对象存储 + FFmpeg 媒体工具**，以单仓库方式开发。第一阶段采用模块化单体，API 与 Worker 共用领域代码但分进程部署；保留后续拆分服务的边界，不在 MVP 阶段承担微服务运维成本。

这里的 FFmpeg 不是后端开发语言，也不要求开发者用 C/C++ 开发媒体算法。它是服务器上的可执行媒体工具，由 Go Worker 通过封装好的少量命令模板调用，用于读取视频信息、转代理视频、抽帧、生成波形和最终合成。业务逻辑、任务状态、权限、费用与接口仍全部使用 Go 编写。

---

## 2. 项目目标与边界

### 2.1 产品目标

用户输入小说、故事梗概、单集剧本或已有素材后，系统能够完成：

1. 剧情推理、逻辑审计、分集与分段规划；
2. 角色、场景、道具、世界观和连续状态沉淀；
3. 文生图、参考图生图、母图与角色锚点管理；
4. 生成可直接交给视频模型的分镜和逐秒导演提示；
5. 调用多个文/图/音/视频模型异步生成视频；
6. 在 Web 时间线内完成拼接、裁切、字幕、配音、音效、转场和基础画面调整；
7. 自动检查规格、时长、连续性、字幕、黑帧、静音、分辨率和导出参数；
8. 导出标准成片和完整制作包，并保留全过程版本与成本数据。

### 2.2 MVP 必须实现

- 账号、项目和素材库；
- 项目圣经：世界观、角色、场景、道具、风格；
- 剧情导入、分集、四段/自定义分段、镜头表；
- 结构化提示词编辑器；
- 文生图/参考图生图的 Provider 接入框架，至少打通一个可用图片模型；
- Seedance 2.0 与 Grok 视频 Provider 框架，至少打通一个真实视频接口；
- 异步任务中心、任务状态和结果回收；
- 生成版本对比、选择、重试与局部重做；
- 单轨/多轨基础时间线、字幕、音频、基础转场；
- 服务端 FFmpeg 导出；
- 《一卒定山河》规则模板和自动 QA。

### 2.3 MVP 暂不实现

- 类似 Premiere/剪映的完整特效生态；
- 多人光标级实时协同编辑；
- 自研基础视频生成模型；
- 数字人训练、声音克隆训练；
- 公开模板市场和模型交易市场；
- 全自动“一键成片后直接发布”，首版仍保留关键人工验收节点；
- 原生移动端专业剪辑。

### 2.4 成功指标

业务指标：

- 一集从剧情进入到可导出初版成片的人工操作步骤减少 50% 以上；
- 因文件错放、角色参考误入时间线、字幕越界、时长超限导致的返工下降 80% 以上；
- 单段失败只重做当前段，整集连锁返工率低于 10%；
- 所有模型调用可追踪输入、输出、耗时、费用、失败原因和采用版本。

产品指标：

- 新用户能在不离开项目工作台的情况下完成“剧本→母图→视频→剪辑→导出”；
- 关键任务状态在 5 秒内显示更新；
- 生成结果到达后自动永久转存，不依赖供应商临时 URL；
- 每个成片可反查到所用剧本版本、提示词版本、参考素材与模型参数。

---

## 3. 《一卒定山河》过往流程复盘

### 3.1 第一阶段：竖屏母图驱动

早期流程是：

1. 每集准备标题卡、4 张剧情母图和 1 张角色参考图；
2. 使用 9:16 接近竖屏的母图生成短视频；
3. 在剪辑软件中裁切、推拉并组合为多个镜头；
4. 最后导入 SRT、配音、音乐和音效；
5. 成片控制在 60 秒内。

已形成的重要规范：

- 标题卡 0—1.5 秒并计入总时长；
- 第一条声音在约 0.3 秒提前进入，避免无声片头；
- 1.5—58.5 秒为剧情，58.5—60 秒为尾帧定格和强钩子；
- 角色参考图明确标记“勿入时间线”；
- 不让模型直接生成汉字，字幕后期烧录；
- 一张母图拆成 2—4 个单动作短镜，降低换脸和穿模概率；
- 人物、道具、伤势、运动方向和光源必须连续。

### 3.2 第二阶段：发现的主要问题

实际制作与审计暴露出以下高频返工点：

| 问题 | 表现 | 根因 | 平台化解决方案 |
|---|---|---|---|
| 画幅不统一 | 标题横版、剧情竖版或素材尺寸混入 | 文件靠人工识别 | 上传即读取媒体元数据，按项目规格阻止错误素材进入时间线 |
| 角色参考误用 | 参考图被当成剧情镜头 | 目录与用途不清 | 资产增加 `usage_role`，角色参考默认不可拖入成片轨 |
| 复杂动作崩坏 | 同一提示要求多人跑、转身、拔刀、说话 | 单段叙事任务过多 | 提示词预检检测动作数量、人物数量与相互冲突运镜 |
| 人脸与服装漂移 | 相邻镜头年龄、脸、伤势或服装改变 | 缺少状态继承 | 角色锚点 + 连续状态表 + 每段自动注入稳定特征 |
| 纸面文字乱码 | 模型重绘令牌、军令、标题 | 模型不擅长稳定文字 | 画面只保留无可读复杂字形，信息由字幕/配音表达 |
| 剧情提前泄底 | 钩子直接给出下一集答案 | 缺少信息边界 | 每集记录“本集可知事实”和“禁止提前揭示事实” |
| 时长超限 | 标题、转场或定格未计入 60 秒 | 多处独立计时 | 统一时间线时钟，导出前硬性时长门禁 |
| 整集重做 | 某段尾帧或人脸失败后全部返工 | 没有段级版本 | 段级任务、段级版本、段级审核和尾帧接力 |
| 接缝跳变 | 人物、车辆、运动方向和光线突变 | 相邻段各自生成 | 使用上一段实际 14.8—15.0 秒清晰帧作为下一段连续参考 |
| 文件散乱 | 提示词、字幕、图片和说明难对应 | 依赖手工目录 | 数据库对象关联，导出时再生成标准目录包 |

### 3.3 第三阶段：横版四分镜标准化

第二至十集后续逐渐收敛为：

- 16:9、1280×720、30fps；
- 每集固定 4 个分段，每段严格 15 秒；
- 分镜头一包含 0—1.5 秒标题卡；
- 每段固定 6 类文件：连续参考/标题、母图、角色参考、逐秒视频提示词、独立 SRT、提示词字幕合并稿；
- 每个 15 秒段只有一个叙事任务；
- 每份提示词 15 条逐秒描述且低于 2500 字；
- 四段直接硬拼，不依赖默认叠化；
- 三个接缝重点检查 14.8—15.2、29.8—30.2、44.8—45.2 秒；
- 失败只重做当前 15 秒段；
- 每集增加连续状态表和 QA 报告。

### 3.4 已验证有效的创作约束

1. **一段一任务**：例如“闭门搭箭→亮铜印”，不能同时完成开门、验印、授职。
2. **证据按链展示**：铜印→蜡痕→蓝绳→暗红札→鞍袋→粮车和活口，避免证物瞬移和观众理解断裂。
3. **抽象情绪转为可见动作**：不写“紧张”，而写“握令手指松开、呼吸变慢、眼神转向证物”。
4. **转场在分镜时设计**：墨尘、铜印、下降矛阵、破旗、车轮、绳索和声音桥，剪辑时不再靠模板补救。
5. **尾帧是正式资产**：上一段通过后再截真实尾帧给下一段，而不是只用母图占位。
6. **数量和状态显式记录**：例如二十人、三十车、三千敌军、俘虏另算、旧粮与新粮分批。
7. **自动检查 + 人工目视并行**：尺寸、时码、文件数、提示词长度可自动检查；脸、手、兵器、受力和构图必须人工复核。

这些规则将直接成为平台内置的“《一卒定山河》项目模板”，而不是仅保存在文档里。

---

## 4. 竞品与模型接入事实边界

### 4.1 Tago Movie 登录后复核结果

本次在不创建项目、不上传素材、不购买套餐和不消耗积分的前提下，对登录后的产品入口进行了只读检查。可确认能力如下。

#### A. 产品与商业结构

- 主导航包含首页、项目中心、资产中心、漫剧培训、推广赚钱、商单对接、使用教程、客服和积分入口；
- 项目中心提供项目搜索和添加项目；
- 采用积分与订阅套餐体系；不同套餐区分品牌水印、Seedance 2.0、并发任务数量、1080P 和画布模式权限；
- 产品不只提供工具，还试图覆盖培训、推广分佣和商业订单对接，形成创作—学习—变现闭环。

#### B. 两种创作模式

- **整剧模式**：一次性规划整部剧，支持输入故事想法、整本小说或完整剧本；
- **逐集模式**：按单集推进，支持直接输入或上传当前集内容；
- 文本文件入口明确支持 TXT、DOC、DOCX；
- 整剧可配置 1—120 集；逐集模式不要求先规划整部剧。

#### C. 按输入成熟度分流

整剧模式提供：

- 故事想法创作：由创作智能体继续完成故事和剧本；
- 小说改编：对小说进行剧本改编；
- 剧本改编：对已有剧本进行改编优化；
- 原版剧本：遵循原内容，不做改编优化。

逐集模式提供：

- 单集故事创作：先把故事生成当集剧本；
- 单集剧本创作：直接进入剧本编辑与后续制作；
- 单集镜头创作：识别镜头稿中的资产后直接进入镜头制作。

这说明“输入材料成熟度”应当是我们创建项目向导的一等字段，不能把所有用户都强迫进入同一 AI 改写流程。

#### D. 创作规格

- 画幅：9:16、16:9、1:1、4:3、3:4、21:9；
- 单集时长：1 分钟内、1—2 分钟、2—3 分钟、3—5 分钟、5—10 分钟；
- 画面风格同时支持平台预设和自定义风格；
- 预设按真人、2D、3D 等分类，包含大量电影、国漫、动画、插画、定格和 3D 渲染风格。

#### E. 全局资产中心

- 资产支持文件夹，并按角色、场景、道具、音色分类；
- 角色支持 AI 生成形象或上传已有图片，区分简要生成描述与详细形象描述；
- 场景和道具支持 AI 扩写设定、选择画面风格，并一次生成 1—6 张候选图片；
- 音色支持 AI 设计多个声音方案，也支持上传 MP3/WAV 参考音频；页面要求参考音频为 3—5 秒清晰人声、最大 20MB；
- 音色上传意味着平台必须有声音授权、敏感素材与删除机制，不能只把它视为普通文件。

#### F. 已知但本轮未进入的能力

- 公开更新确认存在分段镜头重排，支持段内与跨段移动；
- 套餐文案显示存在“画布模式”，但由于本轮不创建项目，未验证其节点、时间线或生成交互，本文不把它写成已确认的剪辑能力；
- 项目内部的剧情生成步骤、模型选择、镜头视频生成、字幕和导出仍需在用户授权创建测试项目后进一步实测。

#### G. 对本项目的直接修改

1. 创建项目向导增加“整剧/逐集”和“故事想法/小说/剧本/镜头稿/原样保留”分流；
2. 规格模型改为任意画幅、时长区间和集数，`一卒定山河` 只是一个模板，不写死全平台；
3. 资产中心首版就纳入角色、场景、道具、音色和文件夹，不把音色放到后期临时补；
4. 视觉风格成为可复用、可自定义、可版本化的项目资产，并支持按媒介类别筛选；
5. 权益系统需能控制模型、并发、分辨率、水印、画布和存储，而不是只记录积分余额；
6. 产品差异化继续放在 Tago 当前入口没有显式展示的深层生产控制：连续状态、证据链、逐秒 ShotSpec、尾帧接力、Take 版本、自动 QA、费用可追溯和可复现导出；
7. 培训、推广和商单不进入 MVP，但数据与导航设计不得妨碍后续扩展创作者商业闭环。

登录入口参考：[Tago Movie 创作工作台](https://tagomovie.bitaihub.com/app)、[项目中心](https://tagomovie.bitaihub.com/projects)、[资产中心](https://tagomovie.bitaihub.com/assets)。

### 4.2 Seedance 2.0

火山引擎公开资料确认 Seedance 2.0 API 已开放，并强调文字、图片、音频、视频多模态输入、参考与编辑能力，以及肖像/版权安全机制。平台应优先以多参考素材和异步任务方式接入，而不是把它当作只有 `prompt` 的单一文生视频接口。

接入策略：

- Provider 能力通过运行时配置读取；
- 具体 endpoint、模型 ID、可用时长、分辨率、参考素材数量和审核规则不写死在前端；
- 支持可信素材或供应商 Asset ID；
- 生成结果完成后立即转存到本平台对象存储；
- 供应商原始响应完整留存到审计字段，但对业务层输出统一结构。

官方参考：

- [Seedance 2.0 API 开放与多模态能力](https://developer.volcengine.com/articles/7628567056649125942)
- [火山方舟可信素材库与真人肖像授权](https://www.volcengine.com/docs/82379/2315856?lang=en)

### 4.3 Seedance 2.5

截至本文编写时，本次官方检索未获得可验证的 Seedance 2.5 正式 API 文档。因此：

- 产品界面可以预留“Seedance 2.5”模型槽位；
- 代码中只新增 Provider/Model 配置，不预设并伪造接口参数；
- 获得真实控制台权限、模型 ID 和文档后完成 capability mapping 与契约测试；
- 若 Seedance 2.5 与 2.0 同一供应商但参数不同，也必须视为独立模型能力配置，不能靠字符串替换上线。

### 4.4 Grok 视频

xAI 官方文档确认 Grok Imagine 视频使用异步流程：提交生成任务获得 `request_id`，之后轮询任务状态。当前官方文档展示 `grok-imagine-video-1.5`，支持文本生成、图生视频、参考生成等能力；生成时长与分辨率受模型和模式约束，生成 URL 为临时地址，必须及时转存。

平台实现时：

- 使用 `/v1/videos/generations` 类异步任务适配；
- 状态统一映射为 `QUEUED/RUNNING/SUCCEEDED/FAILED/EXPIRED/CANCELED`；
- 结果进入下载与转存子任务；
- 记录模型返回的使用量和成本字段；
- 约束由 Provider Capability Registry 驱动。

官方参考：

- [xAI Video Generation](https://docs.x.ai/developers/model-capabilities/video/generation)
- [xAI Grok Imagine Video 模型与价格](https://docs.x.ai/developers/models/grok-imagine-video)

---

## 5. 用户、角色与权限

第一版角色：

| 角色 | 权限 |
|---|---|
| Owner | 项目、成员、模型密钥、预算、删除、导出和发布设置 |
| Director | 剧情、分镜、提示词、素材选版、镜头审核、时间线 |
| Editor | 时间线、字幕、音频、转场、导出，不修改项目级世界观 |
| Reviewer | 查看、批注、通过/驳回，不直接改生产数据 |
| Viewer | 只读预览 |

MVP 可以先只实现 Owner 与 Editor 两级，但数据库从一开始保留项目成员与 RBAC 表，避免以后重构所有查询。

权限作用域：

- 工作区级：成员、模型密钥、总预算；
- 项目级：读写、导出、删除；
- 敏感操作级：删除素材、覆盖正式版本、发布成片、查看供应商原始错误；
- 供应商密钥永不返回浏览器，只由服务端解密调用。

---

## 6. 产品信息架构

### 6.1 一级导航

1. **项目**：项目列表、最近编辑、状态、预算、成片；
2. **创作工作台**：剧情、剧集、分段、分镜、提示词；
3. **素材库**：文件夹、角色、场景、道具、视觉风格、音色、图片、视频、音频、字幕；
4. **生成中心**：图片/视频任务、队列、进度、失败和费用；
5. **剪辑台**：时间线、预览、字幕、音轨、导出；
6. **审片与 QA**：规则结果、批注、对比、通过/驳回；
7. **设置**：模型、密钥、对象存储、模板、成员、预算。

### 6.2 项目工作台布局

- 左侧：剧集树 → 分段 → 镜头；支持拖拽重排和跨段移动；
- 中间：当前编辑器或预览画布；
- 右侧：结构化属性、连续性、参考素材、模型参数与 QA；
- 底部：版本历史或时间线；
- 顶部：项目状态、保存状态、预算、生成任务和导出。

### 6.3 核心用户路径

```text
创建项目
→ 导入故事/剧本
→ 建立项目圣经
→ AI剧情推理与人工确认
→ 分集与四段规划
→ 分镜与逐秒提示
→ 生成/选择母图
→ 段级视频生成
→ 段级审核与尾帧接力
→ 时间线拼接
→ 配音/字幕/音效
→ QA门禁
→ 服务端导出
→ 归档制作包
```

### 6.4 创建项目向导

创建项目不是一个统一大输入框，而是先完成三层选择：

1. **生产模式**：整剧规划或逐集推进；
2. **输入成熟度**：故事想法、小说、故事稿、剧本、镜头稿、原样保留；
3. **输出规格**：画幅、单集时长、集数、视觉风格、默认模型和预算。

向导根据输入成熟度跳过不需要的阶段：镜头稿可以直接进入资产识别和分镜校验；原版剧本只做格式化与风险检查，不进行未经授权的剧情改写。该设计借鉴 Tago Movie 的入口分流，但增加“处理策略预览”，在开始前明确 AI 将改写什么、保留什么。

---

## 7. 模块一：剧情推理、文生图与分镜制作

### 7.1 剧情输入

支持：

- 粘贴故事梗概或单集剧本；
- 上传 TXT、Markdown、DOCX、PDF；
- 分章节导入长篇小说；
- 导入已经拆好的镜头稿并识别角色、场景、道具和台词；
- 从上一集复制状态创建下一集；
- 导入既有角色图、场景图和制作规范。

输入处理策略：

- `IDEA_TO_SERIES`：故事想法扩写为整剧规划；
- `NOVEL_ADAPTATION`：小说改编并保留章节来源映射；
- `SCRIPT_OPTIMIZATION`：优化已有剧本；
- `SCRIPT_PRESERVE`：不改写剧情，只做结构化和 QA；
- `EPISODE_STORY`：单集故事转单集剧本；
- `EPISODE_SCRIPT`：单集剧本直接进入分镜；
- `SHOT_LIST_IMPORT`：镜头稿直接进入资产识别和镜头制作。

处理策略必须写入项目版本，用户可以看到原文、变更摘要和 AI 生成内容，不能静默改变原稿。

导入后分为：

- 原文层：不可变的原始来源；
- 解析层：章节、角色、地点、事件、物件、时间；
- 创作层：项目圣经、分集大纲、剧本、分镜；
- 生产层：提示词、素材、生成任务、时间线。

任何 AI 改写都写入新版本，不覆盖原文。

### 7.2 剧情推理引擎

输出结构：

- 本集目标；
- 主角目标与阻碍；
- 已知事实、疑点与未解问题；
- 因果链和证据链；
- 开场钩子、中段升级、爽点、结尾新问题；
- 必须画面表达的信息；
- 只能用对白/旁白表达的信息；
- 禁止提前揭示的信息；
- 与上一集、下一集的承接点；
- 逻辑风险与修改建议。

剧情 QA：

- 人物是否无依据知道信息；
- 证物是否无来源出现；
- 人数、地点和时间是否矛盾；
- 结尾是否提前回答下一集；
- 主角成长是否有已展示功劳支撑；
- 本集是否包含太多独立事件；
- 对白量是否超过分配时间。

### 7.3 项目圣经

项目级对象：

- 世界观：时代、地理、阵营、制度、禁用元素；
- 视觉风格：画幅、分辨率、色板、材质、镜头语言；
- 角色：身份、年龄、脸型、发型、服装、体型、伤势、性格、声音、负面约束；
- 场景：空间结构、入口、光源、天气、可复用背景；
- 道具：尺寸、材质、缺口、数量、所属和剧情意义；
- 术语表：人名、地名、军职、固定字幕写法；
- 发布模板：标题卡、字幕样式、片尾、导出规格。

### 7.4 连续状态系统

每集开始状态和结束状态至少记录：

- 角色位置、朝向、服装、伤势、污迹、随身物；
- 队伍人数与角色归属；
- 车辆、粮袋、兵器、证据和俘虏数量；
- 场景破坏状态；
- 天气、时辰、主光方向和色温；
- 上一集最后一句信息和最后一个动作；
- 下一集入口。

系统行为：

- 新建下一集时自动复制上一集结束状态；
- 变更前后高亮差异；
- 未经剧情事件支持的年龄、服装、职位或道具变化标为错误；
- 允许人工确认“有意变化”，并记录原因。

### 7.5 分集、分段和分镜

默认提供两种模板：

- 《一卒定山河》：4 段 × 15 秒、总长 60 秒、首段含 1.5 秒标题卡；
- 通用短剧：自定义画幅、总时长、段数和镜头数。

每个分段字段：

- 唯一叙事任务；
- 起始状态和结束状态；
- 时间预算；
- 关键冲突/证据/动作；
- 参考素材；
- 首帧和尾帧要求；
- 禁止在本段发生的内容；
- 状态：草稿、待审、已锁定、生成中、已通过。

每个镜头字段：

- 时间范围；
- 叙事目的；
- 主体与人物数量；
- 起始位置、朝向、视线；
- 景别、机位、焦段；
- 单一主运镜；
- 动作、速度、受力、终止条件；
- 表情与呼吸；
- 主光、色温、阴影；
- 风、尘、火、衣摆等辅助动态；
- 台词、旁白、环境声、音效、音乐；
- 首帧描述、尾帧描述；
- 转场类型；
- 连续性备注；
- 负面约束。

### 7.6 结构化提示词编译器

系统不把提示词当一整块不可理解的文本，而由 ShotSpec 编译生成不同模型格式。

示例：

```json
{
  "segmentId": "seg_ep10_01",
  "durationSec": 15,
  "output": { "aspectRatio": "16:9", "width": 1280, "height": 720, "fps": 30 },
  "task": "归营受阻并亮出真铜印，不完成对证",
  "references": [
    { "assetId": "title_ep10", "role": "opening_frame" },
    { "assetId": "master_gate", "role": "composition" },
    { "assetId": "character_bible_ep10", "role": "identity_only" }
  ],
  "timeline": [
    {
      "from": 2,
      "to": 3,
      "subject": "城墙弓手与林阳队伍",
      "action": "弓手同步搭箭但不放箭",
      "camera": "24mm贴地后拉",
      "light": "冷晨侧光",
      "sound": "弓弦骤响",
      "endState": "箭尖指向队伍"
    }
  ],
  "continuity": {
    "peopleCount": 20,
    "captiveExcluded": true,
    "linYangLeftHand": "旧伤与真铜印",
    "gate": "正门保持关闭"
  },
  "negative": ["无人放箭", "无人拔刀", "禁止可读汉字", "禁止人物复制", "禁止水印"]
}
```

编译器输出：

- 人类可读分镜表；
- Seedance 专用提示词；
- Grok 专用提示词；
- 图片模型母图提示词；
- SRT 草稿；
- 时间线占位片段；
- QA 所需规则数据。

### 7.7 文生图与素材选版

素材类型：

- 角色脸锚点；
- 角色全身设定；
- 多角色关系参考；
- 场景概念图；
- 道具特写；
- 15 秒段母图；
- 标题卡；
- 实际尾帧；
- 失败版本与废弃版本。

生成流程：

1. 从项目圣经和当前状态自动组合提示；
2. 选择模型、比例、分辨率、参考强度和生成数量；
3. 预估费用并预占预算；
4. 提交异步任务；
5. 转存原图并读取尺寸、色彩、透明度和哈希；
6. 生成缩略图和代理图；
7. 多版本并排选择；
8. 标记“采用、候选、废弃、仅参考”；
9. 人工检查脸、手、服装、兵器、文字、构图；
10. 采用版本锁定后才能进入正式视频生成。

图片编辑必须生成新版本，不能覆盖已被视频任务引用的素材。

---

## 8. 模块二：Seedance、Grok 等视频生成

### 8.1 Provider Adapter 架构

统一接口：

```ts
interface VideoProviderAdapter {
  getCapabilities(modelId: string): Promise<ModelCapabilities>;
  validate(input: NormalizedVideoRequest): Promise<ValidationResult>;
  estimateCost(input: NormalizedVideoRequest): Promise<CostEstimate>;
  submit(input: NormalizedVideoRequest, idempotencyKey: string): Promise<ProviderJobRef>;
  getStatus(job: ProviderJobRef): Promise<NormalizedJobStatus>;
  cancel?(job: ProviderJobRef): Promise<void>;
  fetchResult(job: ProviderJobRef): Promise<ProviderResult>;
}
```

统一请求只描述业务意图：

- 模式：text-to-video、image-to-video、reference-to-video、video-edit、video-extend；
- prompt 与 negative prompt；
- 参考素材及 role；
- 时长、比例、分辨率、帧率；
- 首帧、尾帧或视频输入；
- 音频策略；
- 随机种子（供应商支持时）；
- 供应商扩展参数 `providerOptions`。

### 8.2 Capability Registry

每个模型实时/定期维护：

- 可用地区和账号权限；
- 支持的输入模态；
- 支持的生成模式；
- 最小/最大时长；
- 分辨率和画幅；
- 参考图片/音频/视频数量；
- 是否支持首尾帧、编辑、续写、取消；
- 最大 prompt 长度；
- 内容审核要求；
- 价格和币种；
- 并发、速率限制、平均排队时间；
- 结果 URL 有效期。

前端模型表单由能力表驱动，用户不会看到模型不支持的参数。

### 8.3 标准任务状态机

```text
DRAFT
→ VALIDATING
→ BUDGET_RESERVED
→ QUEUED
→ SUBMITTED
→ RUNNING
→ RESULT_FETCHING
→ TRANSCODING_PROXY
→ QA_PENDING
→ SUCCEEDED

任意阶段 → FAILED / EXPIRED / CANCELED
FAILED → RETRY_QUEUED（满足重试规则时）
```

### 8.4 任务幂等与重试

- 幂等键由 workspace、segment、promptVersion、referenceVersion、model、parameters 计算；
- 用户重复点击不重复扣费；
- 网络错误、5xx 和临时限流指数退避重试；
- 参数错误、内容审核拒绝和权限错误不自动重试；
- 自动重试不超过配置次数；
- 每次重试保存供应商错误码与原始响应；
- 支持“复制参数再生成”，作为新版本而不是覆盖旧任务。

### 8.5 批量与依赖生成

任务可以并行，但有依赖的段必须受控：

- 不依赖尾帧时，多个候选版本可并行；
- 分镜二依赖分镜一实际尾帧时，分镜一通过并抽帧后再放行；
- 同集可配置“质量优先串行”和“速度优先并行”；
- 供应商并发、项目预算和用户配额共同决定调度优先级。

### 8.6 结果回收

供应商返回成功后立即执行：

1. 下载临时 URL；
2. 校验 MIME、文件大小、时长、分辨率和可解码性；
3. 计算 SHA-256；
4. 写入对象存储；
5. 生成浏览器预览代理视频；
6. 抽取首帧、尾帧和 contact sheet；
7. 生成波形和音轨信息；
8. 进入自动 QA；
9. 更新费用结算，多退少补预算预占。

### 8.7 版本比较和审核

同一分段允许多个 Take：

- A/B 同步播放；
- 关键帧并排；
- 人脸、构图、动作、尾帧、声音和成本评分；
- 标记采用原因和废弃原因；
- 采用版本变化后提示下游时间线与尾帧参考可能过期；
- 已用于正式导出的版本删除前必须阻止并提示依赖。

---

## 9. 模块三：Web 视频剪辑制作

### 9.1 剪辑台目标

首版定位为“AI 漫剧生产剪辑台”，不复制通用 NLE 的所有能力。重点解决：

- 4×15 秒或自定义分段快速拼接；
- 段内裁切、变速、静帧、基础缩放和平移；
- 镜头跨段拖拽和重排；
- 多版本替换但不丢失入出点；
- 字幕、配音、音乐、音效和声音桥；
- 三个接缝逐帧检查；
- 标准化导出和 QA。

### 9.2 时间线数据模型

轨道：

- V1 主视频；
- V2 叠加图、标题卡、遮罩；
- A1 角色配音；
- A2 环境与音效；
- A3 音乐；
- S1 字幕；
- Marker 审核点、接缝点、钩子点。

Clip 保存：

- source asset/version；
- timeline start；
- source in/out；
- speed；
- transform；
- opacity；
- volume/fade；
- transition；
- freeze frame；
- link group；
- revision。

所有操作写入时间线 JSON 和操作历史，原素材永不修改。

### 9.3 预览策略

- 浏览器使用代理视频，不直接加载超大原片；
- 预览画布优先保证时间线同步和字幕位置，不承诺与最终编码逐像素一致；
- 复杂效果由服务端低清预渲染；
- 音频波形异步生成；
- 长项目按视窗虚拟化时间线元素；
- 自动保存采用增量 patch，并显示保存/冲突状态。

### 9.4 字幕系统

支持：

- 从分镜台词生成 SRT；
- 导入/导出 SRT；
- 分段 SRT 合并并平移时码；
- 字幕样式模板；
- 每屏最多两行、单行字数、标题区禁入、安全区检查；
- 修改台词后标记配音和字幕可能失配；
- 纸面信息转字幕/旁白的专用标识。

《一卒定山河》模板默认：白字、黑描边、底部安全区上移约 8%，首段 0—1.5 秒不叠对白字幕。

### 9.5 配音与音频

- 角色绑定固定 voice profile；
- 台词生成、试听、重生成和采用版本；
- 台词变更触发音频过期提醒；
- 音量、淡入淡出、J-cut/L-cut；
- 音频响度和峰值检测；
- 环境底噪跨镜连续；
- 音乐 ducking：重要对白期间自动压低背景音乐；
- 不在未获授权时提供声音克隆。

### 9.6 转场与接缝

默认以硬切、动作接力、视线接力、声音先行、道具匹配和遮挡切为主。

系统在每个分段边界提供：

- 前后 0.2 秒逐帧预览；
- 并排显示前段尾帧和后段首帧；
- 运动方向、亮度、色温和构图差异提示；
- 一键把前段清晰尾帧保存为下一段连续参考；
- 替换后段 Take；
- 只在确有需要时添加短转场。

### 9.7 导出

导出流程：

1. 冻结时间线快照；
2. 检查素材依赖；
3. 执行预导出 QA；
4. 生成 FFmpeg filter graph；
5. Worker 渲染；
6. 检查输出文件；
7. 生成成片、封面、字幕和制作清单；
8. 将导出物与时间线快照绑定。

默认预设：

- 横版：1280×720 或 1920×1080、30fps、H.264、AAC 48kHz；
- 竖版：1080×1920、30fps、H.264、AAC 48kHz；
- 用户可创建自定义预设；
- 平台发布规格与项目创作规格分离，允许从同一时间线派生多个渠道版本。

---

## 10. 全链路生产流程与质量门禁

### Gate 0：项目立项

输入：项目名称、画幅、时长、语言、风格、目标平台。  
输出：项目模板、目录逻辑、预算和默认模型。  
门禁：规格完整，模型能力与目标规格兼容。

### Gate 1：剧情确认

输入：原文/剧本。  
输出：本集目标、因果链、证据链、已知/未知信息、钩子。  
门禁：无明显逻辑断裂，无提前泄底，对白量可落入时间预算。

### Gate 2：连续状态确认

输入：上一集结束状态。  
输出：本集开始/结束状态差异。  
门禁：人数、伤势、道具、职位、时间和地点变化均有剧情依据。

### Gate 3：分镜锁定

输入：分段大纲。  
输出：镜头表、逐秒提示、字幕草稿、转场链。  
门禁：每段一个任务、动作可生成、提示词不超模型限制、接缝有承接对象。

### Gate 4：母图锁定

输入：图片候选。  
输出：采用母图、角色参考、首/尾帧参考。  
门禁：尺寸正确，脸/手/服装/兵器/文字人工通过，资产用途正确。

### Gate 5：生成前检查

输入：模型、prompt、参考素材、参数。  
输出：规范化请求、费用估算、幂等键。  
门禁：Provider 支持所选参数，预算足够，敏感素材授权状态可用。

### Gate 6：段级视频审核

输入：生成 Take。  
输出：采用/驳回、问题标签、实际尾帧。  
门禁：可解码、时长/比例正确、无严重换脸/穿模/乱码、尾帧可接。

### Gate 7：时间线审核

输入：四段和音频字幕。  
输出：锁定时间线。  
门禁：接缝、字幕、音量、黑帧、静音、总时长通过。

### Gate 8：导出验收

输入：冻结时间线。  
输出：成片和制作包。  
门禁：编码、分辨率、帧率、音频、字幕安全区、片长、文件完整性全部通过。

---

## 11. 自动 QA 规则

### 11.1 确定性规则

《一卒定山河》模板：

- 每集 4 个分段；
- 每段目标时长 15.000 秒；
- 第一段 0—1.5 秒为标题卡；
- 分段提示覆盖 15 个一秒区间；
- 单提示长度低于目标模型上限，历史模板默认 2500 字；
- 分段 SRT 不超过 15 秒，整集 SRT 不超过 60 秒；
- PNG 必须为 1280×720；
- `identity_only` 资产不得出现在导出时间线；
- 接缝检查窗口固定覆盖 14.8—15.2、29.8—30.2、44.8—45.2 秒；
- 57.5—60 秒必须存在具体钩子；
- 标题、定格和转场全部计入总时长。

通用规则：

- 媒体可解码、无零字节；
- 实际时长、分辨率、帧率和声道；
- 黑帧、重复帧、长静音、削波；
- 字幕重叠、倒序、超界、过长和安全区；
- 缺失引用和孤儿素材；
- 时间线片段冲突；
- 导出与冻结快照一致。

### 11.2 AI 辅助规则

- 人物身份相似度异常；
- 年龄、发型、服装、伤势漂移；
- 手指、兵器、绳索、车辆结构异常；
- 现代元素、公众人物相似风险、Logo/水印和乱码；
- 镜头是否完成了禁止提前发生的内容；
- 人数与关键道具数量是否明显不符；
- 开头钩子和结尾钩子是否清楚。

AI 检测只提供风险分和证据帧，不自动替代人工审片。

### 11.3 问题标签

标准标签便于统计和后续提示优化：

`FACE_DRIFT`、`COSTUME_DRIFT`、`EXTRA_PERSON`、`HAND_ERROR`、`WEAPON_DEFORM`、`TEXT_GARBLED`、`WATERMARK`、`MOTION_OVERLOAD`、`AXIS_JUMP`、`LIGHT_MISMATCH`、`BAD_END_FRAME`、`AUDIO_CLIP`、`SUBTITLE_OVERFLOW`、`PLOT_LEAK`、`COUNT_MISMATCH`。

---

## 12. 系统技术架构

### 12.1 总体架构

```text
Web Client
  ├─ 剧情/分镜编辑器
  ├─ 素材与生成中心
  ├─ 审片与版本对比
  └─ Web 时间线
        │ HTTPS + SSE
API / BFF（Go）
  ├─ Auth / Workspace / RBAC
  ├─ Project Bible / Story / Storyboard
  ├─ Asset / Version / Timeline
  ├─ Provider Gateway
  ├─ QA / Review / Budget
  └─ Export Orchestrator
        │
PostgreSQL ─ Redis/Asynq ─ S3兼容对象存储/CDN
                       │
Workers
  ├─ Provider Poller
  ├─ Media Downloader
  ├─ FFmpeg Probe/Proxy/Thumbnail
  ├─ Frame Extractor/Waveform
  ├─ QA Worker
  └─ Export Renderer
```

### 12.2 推荐技术栈

| 层 | 推荐 |
|---|---|
| 代码仓库 | 单仓库；`web`、`cmd/api`、`cmd/worker`、`internal`、`deploy` 分区 |
| Web | React + TypeScript + Vite；若以后需要大量公开 SEO 页面，再单独引入 Next.js |
| UI | Tailwind CSS + 可访问组件库 + 自建时间线组件 |
| 前端状态 | 服务端状态查询库 + 轻量编辑状态；时间线使用独立 Store |
| API | Go + Gin（或 Chi）+ REST/OpenAPI；实时状态使用 SSE |
| 数据访问 | PostgreSQL + pgx + sqlc；数据库迁移使用 Goose/Atlas 二选一 |
| 队列 | Redis + Asynq；任务真相保存在 PostgreSQL，队列只负责调度 |
| 对象存储 | S3 兼容存储；本地开发使用 MinIO |
| 媒体 | FFmpeg/ffprobe，由 Go Worker 通过受控命令模板调用 |
| 预览 | HTMLVideoElement + Canvas/WebGL 叠加层 |
| 鉴权 | 邮箱/第三方登录 + 服务端 Session/JWT；项目 RBAC |
| 日志 | Go 结构化日志 + 错误追踪 + OpenTelemetry |
| 测试 | Go 原生测试、Testcontainers、前端单元测试、Playwright E2E、媒体黄金样本 |
| 部署 | Docker；Web/API/Worker 分进程部署 |

### 12.3 为什么首版不用微服务

- 领域模型仍在快速调整；
- 事务涉及项目、版本、费用和任务，模块化单体更易保证一致性；
- Worker 已经天然与 Web/API 分离，可单独扩容；
- Provider、媒体和导出均通过接口隔离，后续负载增长时可以独立拆分。

### 12.4 实时通信

MVP 使用 SSE 推送：

- 生成任务状态；
- 导出进度；
- QA 结果；
- 预算变化。

时间线编辑保存使用普通 API。多人实时协作进入后续版本时再引入 WebSocket/CRDT，避免首版复杂化。

### 12.5 后端语言选择结论

技术上的“最优”必须同时考虑性能、生态、开发速度和项目维护者的能力。原 NestJS 方案适合全 TypeScript 团队，但不是本项目当前的最优方案。

| 方案 | 优点 | 缺点 | 结论 |
|---|---|---|---|
| Go | 协程适合大量异步轮询和 Worker；静态编译、部署轻；调用 FFmpeg 和处理文件流直接；API 性能与资源占用好 | 企业后台脚手架不如 Java/Laravel 丰富 | **本项目推荐主路线** |
| Java + Spring Boot | 事务、权限、监控、队列和企业生态成熟；团队扩张后规范性强 | 内存与部署较重；媒体 Worker 和大量轻量轮询的代码更啰嗦 | 如果维护者 Java 明显最熟，可作为等价稳妥备选 |
| PHP + Laravel | CRUD、后台、鉴权、队列和开发速度快 | 常驻媒体 Worker、大文件流和高并发任务编排不如 Go 自然 | 可做管理后台；不推荐单独承载全部媒体处理 |
| NestJS | 前后端统一 TypeScript，生态完整 | 维护者不熟悉 Node 后端，会增加长期学习与排障成本 | 本项目不再作为主路线 |

最终推荐：**Go 模块化单体 API + Go Worker**。如果后续团队以 Java 工程师为主，可以把 API 改为 Spring Boot，但 Provider 协议、数据库、队列、对象存储和 FFmpeg 边界不需要改变。

### 12.6 FFmpeg 的使用边界

FFmpeg 在本项目只承担六类确定任务：

1. `ffprobe` 读取时长、分辨率、帧率、编码和音轨；
2. 把供应商原片转成浏览器易播放的低码率代理；
3. 抽取首帧、尾帧和接缝检查帧；
4. 生成音频波形或抽取音轨；
5. 合成视频、字幕、配音、音乐与基础转场；
6. 对最终文件做标准化编码。

实施上不允许业务代码到处拼接命令字符串。建立 `media` 包，把命令建模为 Go 结构体并采用参数数组调用，限制输入路径、输出路径、编码器和滤镜白名单；保存命令摘要、退出码与日志，编写媒体黄金样本测试。这样使用者主要维护 Go 代码，只需理解少数经过测试的媒体参数。

如果完全不使用 FFmpeg，就需要购买云转码/媒体处理服务或自行开发解码、合成与编码能力。前者成本和供应商绑定更高，后者明显不可行。因此“使用 FFmpeg，但把它隔离封装”是本项目性价比最高、风险最低的做法。

---

## 13. 单仓库目录建议

```text
web/                       # React + TypeScript 用户界面
  src/
  tests/
cmd/
  api/                     # Go API 进程入口
  worker/                  # 队列、Provider轮询、媒体与QA入口
  migrate/                 # 数据库迁移工具入口
internal/
  auth/                    # 登录、Session/JWT、RBAC
  project/                 # 项目与项目圣经
  story/                   # 剧情、连续状态、分集与分镜
  asset/                   # 素材、版本、对象存储
  generation/              # 统一生成任务与成本
  provider/
    seedance/              # Seedance适配器
    xai/                   # Grok适配器
    mock/                  # 本地和测试Provider
  prompt/                  # ShotSpec与模型提示编译
  media/                   # ffprobe、代理、抽帧、波形、渲染
  qa/                      # 确定性规则和报告
  timeline/                # 时间线数据结构与操作
  render/                  # 导出任务编排
pkg/                       # 确有跨项目价值时才放公共Go包
api/
  openapi.yaml             # 前后端统一接口契约
db/
  migrations/
  queries/                 # sqlc SQL
deploy/
  docker/
  compose/
  observability/
docs/
  architecture/
  adr/                     # 重要架构决策记录
```

---

## 14. 核心数据模型

### 14.1 租户与项目

`users`：用户。  
`workspaces`：工作区。  
`workspace_members`：成员与角色。  
`projects`：项目规格、模板、语言、状态、预算。  
`project_members`：项目权限。  
`project_settings`：字幕、导出、默认模型、规则模板。

### 14.2 创作领域

`source_documents`：原始故事和文件。  
`story_versions`：解析/改写版本。  
`project_bible_entries`：世界观、角色、场景、道具、术语。  
`characters` 与 `character_versions`：角色锚点和版本。  
`locations`、`props`、`factions`：结构化设定。  
`visual_styles` 与 `visual_style_versions`：平台预设、自定义风格、示例图和提示模板。  
`voice_profiles` 与 `voice_profile_versions`：AI 设计音色、供应商 voice ID、参考音频与授权状态。  
`episodes`：集数、标题、目标、状态和总时长。  
`episode_versions`：剧情版本。  
`continuity_snapshots`：开始/结束连续状态。  
`plot_facts`：已知事实、未知事实、禁泄露事实。  
`segments`：分段、排序、任务、时长和状态。  
`shots`：镜头级导演字段。  
`dialogue_lines`：角色、文本、时间、情绪、字幕和配音关联。  
`prompt_versions`：结构化输入、编译文本、目标模型和版本。

### 14.3 素材与生成

`assets`：逻辑素材与用途。  
`asset_versions`：实际文件、尺寸、时长、哈希、来源。  
`asset_folders` 与 `asset_folder_items`：工作区/项目素材文件夹和归类。  
`asset_relations`：由哪个 prompt、参考图、任务或视频派生。  
`provider_accounts`：加密后的供应商凭证引用。  
`provider_models`：模型能力和价格快照。  
`generation_jobs`：统一任务。  
`provider_jobs`：供应商任务 ID、原始状态和响应。  
`generation_attempts`：重试记录。  
`cost_ledger`：预占、实际费用、退款和人工调整。  
`reviews`、`review_comments`：采用/驳回和批注。  
`qa_runs`、`qa_issues`：规则结果与证据帧。

### 14.4 时间线与导出

`timelines`：剧集时间线。  
`timeline_revisions`：不可变快照。  
`tracks`：轨道。  
`clips`：片段引用和效果参数。  
`subtitle_cues`：字幕。  
`markers`：接缝、钩子、批注、QA 点。  
`render_jobs`：导出任务。  
`deliverables`：成片、封面、SRT、制作包、清单。

### 14.5 权益与订阅

`plans`：套餐定义。  
`plan_entitlements`：可用模型、并发、最大分辨率、水印、画布、存储等权益。  
`workspace_subscriptions`：当前套餐及有效期。  
`credit_wallets` 与 `credit_ledger`：积分余额和不可变流水。  
`usage_quotas`：日/月/并发限制。

权益判断必须在服务端执行；前端隐藏按钮只用于体验，不是安全控制。模型 Provider 的真实费用和用户积分是两套账：前者用于成本核算，后者用于产品计费，不能混用一个字段。

### 14.6 关键约束

- 所有排序使用可稳定重排的 `sort_key`，避免拖拽时重写整表；
- `asset_version` 不可变；
- 采用版本使用显式外键，不依赖“最新一条”；
- prompt、生成任务和导出均保存输入快照，保证可复现；
- 删除逻辑素材默认软删除，物理文件由保留策略异步清理；
- 费用台账只追加，不原地修改历史账目；
- 状态迁移通过领域服务校验，禁止任意字符串更新。

---

## 15. API 设计

REST 资源建议：

```text
POST   /v1/projects
GET    /v1/projects/:projectId
PATCH  /v1/projects/:projectId

POST   /v1/projects/:projectId/sources
POST   /v1/projects/:projectId/story/analyze
GET    /v1/projects/:projectId/bible
PUT    /v1/projects/:projectId/bible/:entryId

POST   /v1/projects/:projectId/episodes
POST   /v1/episodes/:episodeId/plan
GET    /v1/episodes/:episodeId/continuity
PUT    /v1/episodes/:episodeId/continuity

POST   /v1/episodes/:episodeId/segments
PATCH  /v1/segments/:segmentId
POST   /v1/segments/reorder
POST   /v1/segments/:segmentId/shots
POST   /v1/segments/:segmentId/prompts/compile

POST   /v1/assets/uploads/init
POST   /v1/assets/uploads/complete
GET    /v1/assets/:assetId/versions
POST   /v1/assets/:assetId/select-version
GET    /v1/asset-folders
POST   /v1/asset-folders
GET    /v1/visual-styles
POST   /v1/visual-styles
GET    /v1/voice-profiles
POST   /v1/voice-profiles

GET    /v1/providers/models
POST   /v1/generations/estimate
POST   /v1/generations
GET    /v1/generations/:jobId
POST   /v1/generations/:jobId/cancel
POST   /v1/generations/:jobId/retry

POST   /v1/segments/:segmentId/reviews
POST   /v1/qa/runs
GET    /v1/qa/runs/:runId

GET    /v1/timelines/:timelineId
PATCH  /v1/timelines/:timelineId
POST   /v1/timelines/:timelineId/revisions
POST   /v1/timelines/:timelineId/renders
GET    /v1/renders/:renderId

GET    /v1/me/entitlements
GET    /v1/workspaces/:workspaceId/credits/ledger
GET    /v1/events/stream
```

API 规则：

- 所有修改接口支持乐观锁 `revision`；
- 所有创建生成/导出任务支持 `Idempotency-Key`；
- 大文件浏览器直传对象存储，API 只签发短期上传地址；
- 返回统一错误结构：`code`、`message`、`fieldErrors`、`retryable`、`traceId`；
- 供应商原始错误只对授权用户可见；
- OpenAPI 作为前后端契约并自动生成类型客户端。

---

## 16. 素材存储与媒体处理

### 16.1 对象存储键

不要使用用户文件名作为唯一键。建议：

```text
workspace/{workspaceId}/project/{projectId}/asset/{assetId}/version/{versionId}/original.ext
workspace/{workspaceId}/project/{projectId}/asset/{assetId}/version/{versionId}/proxy.mp4
workspace/{workspaceId}/project/{projectId}/asset/{assetId}/version/{versionId}/thumb.webp
workspace/{workspaceId}/project/{projectId}/render/{renderId}/final.mp4
```

### 16.2 上传

- 分片直传；
- MIME 白名单和最大尺寸；
- 上传完成后服务端校验哈希和元数据；
- 图片生成缩略图；
- 视频生成代理、封面、首尾帧、波形；
- 原文件私有，预览使用短期签名 URL 或鉴权 CDN。

### 16.3 生命周期

- 被正式版本引用的素材不可自动删除；
- 临时上传和失败任务产物设置清理周期；
- 供应商临时结果先转存再标记任务成功；
- 用户删除项目先进入回收站，物理清理延迟执行并记录审计日志。

---

## 17. 成本与配额

### 17.1 费用流程

```text
参数校验 → 估价 → 预占额度 → 提交 → 供应商结算 → 实际入账 → 释放差额
```

### 17.2 成本维度

- 工作区、项目、剧集、分段；
- 模型与供应商；
- 生成模式；
- 分辨率、时长和候选数量；
- 成功、失败、供应商退款；
- 最终采用 Take 的成本与全部抽卡成本。

### 17.3 控制策略

- 提交前展示本次估价与项目累计费用；
- 项目硬预算和软提醒；
- 同一幂等请求不重复扣费；
- 批量任务在超过阈值时二次确认；
- 自动重试有单独次数和费用上限；
- 提供“草稿模型→高质量模型”的路由策略；
- 将失败标签与模型/提示版本关联，统计最常见返工原因。

### 17.4 权益控制

参考 Tago Movie 已验证的套餐维度，权益引擎至少支持：

- 可用模型/模型等级；
- 同时运行的生成任务数；
- 最大输出分辨率；
- 是否去水印；
- 是否开放画布/高级剪辑；
- 存储、导出次数和项目数量；
- 是否允许单独购买积分。

MVP 阶段可以只在后台配置权益而不开发完整购买页面，但 API 和 Worker 从第一天就必须检查权益与并发，避免后续把商业规则散落在前端按钮和 Provider 代码里。

---

## 18. 安全、合规与内容治理

- API Key 使用 KMS/密钥管理加密，只在 Worker 内短暂解密；
- 数据库、对象存储和日志按工作区隔离；
- 所有下载、预览和上传 URL 短期有效；
- 对项目删除、成员权限、模型密钥、正式版本和导出记录审计日志；
- 用户上传真人肖像、声音、版权素材时记录授权来源和适用范围；
- 对需要供应商 Asset ID 的可信素材建立映射；
- 不绕过供应商内容审核；
- 供应商失败原因区分：参数、权限、限流、审核、内部错误；
- 日志不得记录完整 API Key、签名 URL、原始敏感提示和未脱敏个人信息；
- 支持工作区数据保留策略和项目导出/删除；
- AI QA 对“公众人物相似”只作风险提示，商业发布前仍需人工复核。

---

## 19. 可观测性与运维

### 19.1 必须监控

- API 请求量、错误率、P95/P99；
- 任务队列长度、等待时间和失败率；
- 各 Provider 成功率、平均时长、超时和限流；
- 结果下载失败和临时 URL 过期；
- FFmpeg 失败、导出耗时和 Worker 资源；
- 对象存储容量与流量；
- 费用估计偏差；
- 自动 QA 各问题标签分布。

### 19.2 追踪

`traceId` 贯穿：浏览器操作 → API → 队列 → Provider Job → 下载 → QA → 导出。

### 19.3 告警

- Provider 连续失败；
- 队列堆积超过阈值；
- 临时结果未在有效期内转存；
- 成本异常增长；
- 导出失败率升高；
- 数据库连接或对象存储异常。

---

## 20. 测试策略

### 20.1 单元测试

- 状态机迁移；
- 时间码和 SRT 平移；
- 提示词编译；
- 费用估算与台账；
- Provider 状态映射；
- 时间线裁切、重排和版本替换；
- 《一卒定山河》QA 规则。

### 20.2 集成测试

- 数据库事务；
- Redis 队列与 Worker；
- 对象存储上传/下载；
- Provider 沙箱/录制响应；
- FFmpeg 探测、代理与导出；
- 临时 URL 过期前转存。

### 20.3 契约测试

每个 Provider 必须有：

- 能力发现测试；
- 请求映射快照；
- 成功、失败、过期、限流和权限错误；
- 原始响应变化检测；
- 模型 ID 不可用时的降级提示。

### 20.4 E2E

关键路径：

1. 创建项目；
2. 导入剧本；
3. 生成四段；
4. 选择母图；
5. 提交模拟视频任务；
6. 采用 Take；
7. 生成尾帧并放行下一段；
8. 时间线重排；
9. 导入字幕；
10. QA；
11. 导出并下载制作包。

### 20.5 黄金样本

从《一卒定山河》第十集建立固定测试夹具：

- 4 段 × 15 秒；
- 20 人、俘虏另算；
- 旧粮与 30 车新粮分开；
- 真铜印、伪令、蓝绳、暗红札证据链；
- 第 4 段钩子完整保留；
- 检测无依据“被绑”“提前开门”“提前升伍长”等错误。

---

## 21. 开发环境、CI/CD 与发布

### 21.1 环境

- Local：Docker Compose 提供 PostgreSQL、Redis、MinIO；Provider 默认 Mock；
- Dev：共享开发环境，使用测试存储和低额度模型账号；
- Staging：与生产同构，执行真实小额端到端测试；
- Production：独立数据库、对象存储、密钥和队列。

### 21.2 CI

每次提交执行：

- 类型检查；
- lint/format；
- 单元测试；
- 数据库 schema 检查；
- API 契约生成差异；
- 构建 Web/API/Worker；
- 关键 E2E（合并前或夜间）。

### 21.3 数据库迁移

- 迁移文件进入版本库；
- 生产迁移先备份并在 Staging 演练；
- 大表变更采用向后兼容的 expand/contract；
- 不允许应用启动时无审查自动修改生产 schema。

### 21.4 发布

- Web、API、Worker 镜像分开；
- Provider 新模型通过 feature flag 灰度；
- 数据结构和 Provider 契约变更可回滚；
- 导出 Worker 可根据队列独立扩容。

---

## 22. 分阶段开发计划

以下顺序是技术依赖顺序，不是简单页面清单。每个阶段都必须形成可演示、可测试的纵向切片。

### 阶段 0：工程底座

交付：

- 单仓库工程、Web/API/Worker；
- PostgreSQL、Redis、对象存储；
- 登录、工作区、项目；
- OpenAPI、日志、错误结构、环境配置；
- CI 和基础部署。

验收：创建项目、上传素材、Worker 处理并回写元数据。

### 阶段 1：剧情与项目圣经

交付：

- 原文导入；
- 整剧/逐集和输入成熟度分流；
- 项目圣经；
- 角色、场景、道具；
- 剧情分析、分集和连续状态；
- 版本历史。

验收：导入《一卒定山河》第十集资料，生成并人工确认连续状态表。

### 阶段 2：分段、分镜与提示词

交付：

- 剧集树、4×15 秒模板；
- 分段与镜头编辑器；
- 拖拽重排和跨段移动；
- ShotSpec；
- 提示词编译和 SRT 草稿；
- 生成前规则检查。

验收：系统输出与历史制作包结构等价的四段提示与字幕，规则检查全部通过。

### 阶段 3：图片生成与素材版本

交付：

- 图片 Provider；
- 资产文件夹、视觉风格和角色/场景/道具/音色分类；
- 参考图、母图、角色锚点；
- 候选对比、采用/废弃；
- 缩略图、元数据、血缘关系；
- 基础人工 QA 表单。

验收：从分镜生成多张母图并锁定正式版本，旧版本可回滚。

### 阶段 4：视频 Provider 与任务中心

交付：

- Provider Core；
- Seedance 2.0 适配器；
- Grok 适配器；
- Seedance 2.5 配置槽位但不上假接口；
- 异步队列、轮询、转存、代理、抽帧；
- 费用与预算；
- Take 对比和段级审核。

验收：至少一个真实 Provider 完成图生视频闭环；模拟器覆盖另一 Provider 的全部状态。

### 阶段 5：Web 剪辑台

交付：

- 视频/音频/字幕轨；
- 裁切、重排、替换版本；
- 标题卡、静帧、基础转场；
- 音量与淡入淡出；
- 接缝检查和实际尾帧接力；
- 自动保存和时间线版本。

验收：四段可直接拼接，替换任一 Take 不破坏其他片段。

### 阶段 6：QA 与服务端导出

交付：

- 确定性 QA；
- FFmpeg 导出；
- 成片检查；
- SRT、封面、制作清单和标准目录包；
- 《一卒定山河》黄金样本回归。

验收：输出 60 秒标准成片和完整制作包，所有硬规则可自动证明通过。

### 阶段 7：生产化

交付：

- RBAC 完整化；
- 审计、安全、密钥管理；
- 监控、告警、备份；
- 供应商灰度和容灾；
- 性能与成本优化；
- 用户引导和项目模板。

验收：Staging 全链路演练通过，可受控开放真实用户。

---

## 23. 每个功能的开发工作流

后续实施统一遵循：

1. 明确用户故事和不做范围；
2. 先写领域对象、状态与不变量；
3. 定义 API/OpenAPI；
4. 写数据库迁移；
5. 写单元和集成测试；
6. 实现 API/Worker；
7. 实现前端；
8. 用真实或黄金样本走通；
9. 做失败、重试、权限、空状态和加载状态；
10. 更新架构决策和验收记录；
11. 通过自动测试与人工演示后合并。

禁止“先把页面画完，再临时拼后端状态”。生成类产品最容易因状态、版本、幂等和费用模型缺失而返工，必须先定义领域和任务状态机。

---

## 24. 非功能验收标准

### 性能

- 普通读接口 P95 目标低于 500ms（不含模型与导出）；
- 项目工作台首屏可用时间目标低于 2.5 秒；
- 任务状态最大可见延迟 5 秒；
- 大素材使用直传和代理，API 不转发整个文件；
- 1 小时项目的时间线仍可通过虚拟化保持可编辑。

### 可靠性

- 生成任务提交幂等；
- Worker 重启后任务可恢复；
- 供应商临时 URL 完成后必须转存；
- 时间线每次正式导出绑定不可变快照；
- 数据库每日备份并定期恢复演练。

### 可用性

- 所有长任务均显示阶段、进度、预计等待和失败原因；
- 空状态给出下一步；
- 危险操作可恢复或二次确认；
- 模型不支持的参数直接隐藏/禁用并解释原因；
- 键盘可完成常见剪辑和审核操作。

---

## 25. 风险清单与应对

| 风险 | 影响 | 应对 |
|---|---|---|
| Seedance 2.5 接口不确定 | 开发写死后返工 | 只预留 Provider/Model 配置，拿到正式文档后实现 |
| 供应商参数与价格频繁变化 | 表单和估价失真 | Capability Registry + 价格快照 + 契约测试 |
| 供应商结果 URL 过期 | 成功任务却丢素材 | 成功后立即下载转存，未转存不标记最终成功 |
| 视频生成耗时长 | 用户误以为卡死 | 异步队列、SSE、阶段状态、可取消、通知 |
| 模型生成随机性 | 抽卡成本与返工 | Take 版本、失败标签、段级重做、先低成本预览 |
| Web 剪辑性能 | 大量轨道卡顿 | 代理视频、虚拟化、增量保存、服务端导出 |
| 角色一致性仍不可靠 | 连载质量下降 | 角色锚点、状态注入、参考强度、QA 证据帧 |
| 自动剧情推理幻觉 | 逻辑错误 | 原文引用、事实层、禁泄露事实、人工 Gate |
| 版权/肖像/声音风险 | 商用受阻 | 授权元数据、可信素材、审计、发布前人工复核 |
| 成本失控 | 用户损失 | 估价、预占、预算、幂等、批量确认、重试上限 |
| 过早做复杂微服务 | 开发和运维拖慢 | 模块化单体 + 独立 Worker，按负载再拆分 |

---

## 26. 从《一卒定山河》旧资料迁移

开发到数据导入阶段时，建议提供一次性迁移器：

1. 扫描“第 X 集/分镜头一至四”目录；
2. 识别标题页、母图、角色参考、尾帧参考、提示词、SRT 和合并稿；
3. 读取 PNG 尺寸、视频元数据和文件哈希；
4. 将制作说明解析为 episode/segment/continuity；
5. 将逐秒表解析为 ShotSpec；
6. 将角色参考标记为 `identity_only`；
7. 将 SRT 写入 subtitle cues；
8. 执行《一卒定山河》QA；
9. 生成导入报告，列出无法自动识别的文件；
10. 原文件只读保留，不在迁移时覆盖或移动。

第十集可作为第一份迁移样本，因为它同时具备：制作说明、连续状态、4 段提示、4 段字幕、整集字幕、母图、角色参考、尾帧参考和 QA 报告。

---

## 27. 评审时需要确认的产品决策

本文已给出推荐默认值。若无异议，开发按推荐项执行。

1. **首发用户**：推荐先做单人/小团队专业创作者，不先做公开社区。
2. **默认画幅**：平台支持自定义；《一卒定山河》模板默认 16:9、1280×720。
3. **默认时长**：模板固定 4×15 秒，通用项目可改。
4. **视频接入顺序**：先 Seedance 2.0，再 Grok；Seedance 2.5 等正式接口确认后接入。
5. **图片模型**：保持 Provider 中立，开发前选择一个真实账号打通。
6. **剧情模型**：保持 Provider 中立，先打通一个支持结构化 JSON 输出的模型。
7. **剪辑范围**：首版做漫剧必需能力，不追求复制完整剪映。
8. **部署区域**：需根据目标用户、模型可用区和合规要求在开工前固定。
9. **计费形态**：MVP 先做内部成本台账和预算，不先做对外充值结算。
10. **协作**：首版实现成员与审片权限，不做多人实时共同编辑。
11. **登录**：首版邮箱/第三方登录二选一或并存，取决于部署环境。
12. **Tago 深度对比**：如果需要完整复刻/差异分析，需要在可控浏览器中重新登录后补一轮后台信息架构审计。

---

## 28. 开工定义（Definition of Ready）

满足以下条件即可开始阶段 0 开发：

- 本文总体架构和 MVP 边界获得确认；
- 明确首个真实图片模型和首个真实视频模型账号；
- 确认本地优先还是直接部署开发环境；
- 确认首发登录方式；
- 明确对象存储选择或允许先用本地 MinIO；
- 明确是否立即导入《一卒定山河》第十集作为黄金样本。

若这些外部条件尚未准备好，也可以先使用 Mock Provider、MinIO 和本地账号系统开发阶段 0—2，不阻塞领域和界面实现。

---

## 29. 最终建议

第一版最重要的不是同时接入最多模型，而是让以下闭环真正稳定：

```text
一集剧情
→ 可审计的四段结构
→ 稳定角色与母图
→ 一个真实视频模型
→ 段级版本与尾帧接力
→ Web时间线
→ 自动QA
→ 可复现导出
```

当这个闭环能用《一卒定山河》第十集完整跑通，再扩展 Seedance 2.5、更多图片模型、高级剪辑和团队协作，技术风险和产品返工会显著更低。
